# Copy this file to `.env.local` if the project later needs local environment values.
# At the moment the app does not require any runtime environment variables.

# Example:
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
